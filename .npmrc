# npm 10.9's peer-set resolver crashes on vitest 5's optional-peer graph
# ("Cannot read properties of null (reading 'edgesOut')"). legacy-peer-deps
# sidesteps it. Safe to drop once on npm >= 11.
legacy-peer-deps=true
